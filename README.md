# Correccion de errores sobre patch

La ultima version del proyecto es la rama `patch`.

Esta rama parte de `patch` y contiene una correccion de errores puntual:

- permite el acceso publico a `GET /api/marcas/**`, necesario para el catalogo;
- corrige errores de lint del frontend.

Esta entrega corresponde a una correccion de errores y queda marcada como entregada "tarde".
